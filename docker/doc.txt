#!/bin/bash
sudo apt update
sudo apt install -y docker.io
sudo docker pull httpd
echo "<h1>Welcome to Apache - YourName</h1>" > index.html
sudo docker run -d -p 80:80 -v $(pwd)/index.html:/usr/local/apache2/htdocs/index.html httpd

