#!/bin/bash

sudo apt update
sudo apt install -y docker.io mysql-server
sudo systemctl start docker
sudo systemctl enable docker

sudo systemctl start mysql
sudo mysql -e "CREATE DATABASE demo_db; SHOW DATABASES;"
